<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chad.program2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TopLevelActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OrderActivity"
            android:label="@string/create_order"
            android:parentActivityName=".TopLevelActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TopLevelActivity" />
        </activity>
        <activity android:name=".ShoeCategoryActivity" />
        <activity android:name=".ShoeActivity"></activity>
        <activity android:name=".ApparelCategoryActivity" />
        <activity android:name=".ApparelActivity"></activity>
        <activity android:name=".AccessoriesCategoryActivity" />
        <activity android:name=".AccessoriesActivity"></activity>

    </application>


</manifest>